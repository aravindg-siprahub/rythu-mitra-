[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"
workingDirectory = "backend"

[deploy]
startCommand = 'sh -c "celery -A rythu_mitra worker -l info & gunicorn rythu_mitra.wsgi:application --bind 0.0.0.0:$PORT"'
