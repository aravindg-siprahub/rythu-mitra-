server {
    listen 80;
    server_name 16.170.172.200 rythumitra.com www.rythumitra.com;
    
    # AI Image Upload Size
    client_max_body_size 50M;

    # Gzip
    gzip on;
    gzip_types text/plain application/json text/css application/javascript;

    # Certbot Challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # API Routing
    location /api/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health Check
    location /health/ {
        proxy_pass http://backend:8000/health/;
        access_log off;
    }

    # Admin Panel
    location /admin/ {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
    }

    # Static Files
    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
    }

    # Media Files
    location /media/ {
        alias /app/media/;
    }

    # Frontend (Catch-All)
    location / {
        proxy_pass http://frontend:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}

# HTTPS Block (Template - Activate after SSL)
# server {
#     listen 443 ssl;
#     server_name rythumitra.com www.rythumitra.com;
#     ssl_certificate /etc/letsencrypt/live/rythumitra.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/rythumitra.com/privkey.pem;
#     client_max_body_size 50M;
#     location / { proxy_pass http://frontend:80; ... }
# }
